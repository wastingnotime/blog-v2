# Implementation

- kept arc-page discovery guidance in `build_arc_page(...)` inside
  `src/app/application/use_cases/build_site.py`
- continued linking arc detail pages back to the currently available
  publication routes, including the archive and search surfaces
- preserved the shared chrome, episode listings, and narrative navigation
  behavior on arc pages
- kept the existing arc-page integration assertions aligned with the generated
  discovery copy and links
