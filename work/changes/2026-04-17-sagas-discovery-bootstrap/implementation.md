# Implementation

- kept the sagas hub discovery guidance in `build_sagas_index_page(...)` in
  `src/app/application/use_cases/build_site.py`
- continued linking the sagas hub to archives and search alongside the active
  saga list
- preserved the existing shared chrome and start-reading behavior for active
  sagas
- kept the sagas-page integration assertions aligned with the generated
  discovery copy and links
